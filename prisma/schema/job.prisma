model JobModel {
  id       String        @id @default(uuid()) @db.Uuid
  status   JobStatusEnum
  type     JobTypeEnum
  entityId String?
  error    String?
}

enum JobStatusEnum {
  PENDING
  DONE
  FAILED
}

enum JobTypeEnum {
  StartVM
  StopVM
  CreateVM
  AttachVolume
  AttachIP
}

